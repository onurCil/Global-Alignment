Script used to calculate the global alignment scores of DNA sequences.

Usage:

global.py input.fasta scoring.txt

Where input.fasta is the two sequences to be aligned and scoring.txt is a table of scores for each match. This script uses a constant gap penalty of -1.
